# 웹 개발 기초

## 이 영역에서 다룰 내용

- 브라우저가 HTML·CSS·JavaScript를 실행하는 과정
- HTTP 요청·응답, 상태 코드, JSON
- Promise와 `async/await`
- 인증 상태와 브라우저 저장소
- 클라이언트에서 발생하는 로딩·실패·재시도 상태

## CodeTrip 적용

CodeTrip은 브라우저에서 React 애플리케이션을 실행하고, Firebase SDK와 여러 외부 API를 호출한다. 따라서 네트워크 지연과 실패가 정상적인 상태임을 전제로 화면 상태를 설계해야 한다.
