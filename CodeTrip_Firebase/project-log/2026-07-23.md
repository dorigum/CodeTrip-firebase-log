# CodeTrip 개발 로그 — 2026-07-23

이 문서는 2026-07-23에 진행된 Gemini 기반 AI 여행 코스 기능 보강, Firebase Auth/API key 트러블슈팅, 위시리스트 저장 구조 개선, 비회원 홈 UX 개편, 회원 전용 접근 안내 및 브랜드 UI 정리 작업을 기록한 문서입니다.

---

## 1. 작업 배경

Gemini API 기반 AI 여행 코스 생성 기능이 로컬에서 정상 호출되기 시작하면서, 실제 사용자 흐름 기준의 테스트를 진행했습니다.

테스트 중 다음 문제가 확인되었습니다.

```text
1. Gemini가 관광공사 API에 등록되지 않은 장소 위주로 코스를 생성함
2. TourAPI contentId가 없는 AI 생성 장소는 위시리스트 카드로 저장되지 않고 메모에만 저장됨
3. 위시리스트 폴더 기반으로 코스를 생성해도 기존 폴더가 아니라 동일한 새 폴더가 다시 생성됨
4. AI 플래너 입력 화면에서 지역/예산/생성 기준에 대한 설명이 부족해 사용자가 입력 기준을 이해하기 어려움
5. Firebase API key 제한 설정으로 로그인 API가 차단되는 문제가 발생함
```

오늘 작업은 AI 플래너 기능을 공모전 제출 관점에서 더 설득력 있게 만들기 위해, **관광공사 API 데이터 우선 활용**, **위시리스트 폴더 기반 코스 생성**, **기존 폴더 저장 보정**, **입력 안내 강화**를 중심으로 진행했습니다.

---

## 2. Firebase API key 및 로그인 트러블슈팅

### 발생 증상

로그인 시 다음 오류가 발생했습니다.

```text
Firebase: Error (auth/requests-to-this-api-identitytoolkit-method-google.cloud.identitytoolkit.v1.authenticationservice.signinwithpassword-are-blocked.)
POST https://identitytoolkit.googleapis.com/... 403 Forbidden
```

### 원인 분석

처음에는 Gemini API key 문제와 혼동될 수 있었으나, 실제 원인은 Firebase Authentication 로그인 API가 사용하는 **Identity Toolkit API**가 API key 제한 설정에 의해 차단된 것이었습니다.

확인한 내용:

```text
Google Cloud Credentials에 Browser key (auto created by Firebase)가 2개 존재
2026년에 생성된 Browser key는 로그인 호출에서 403 발생
2019년에 생성된 Browser key를 .env의 VITE_FIREBASE_API_KEY로 사용하자 로그인 정상 동작
Firebase 콘솔에 표시되는 Web App API key와 실제 로컬 .env에서 사용하는 key가 다를 수 있음
Firebase Auth 로그인은 Gemini API key가 아니라 Firebase Browser key와 Identity Toolkit API 제한 설정의 영향을 받음
```

### 판단 기준

```text
Gemini 호출 실패
- generativelanguage.googleapis.com
- VITE_GEMINI_API_KEY
- Gemini API, quota, 모델명, 서버 상태 확인

Firebase 로그인 실패
- identitytoolkit.googleapis.com
- VITE_FIREBASE_API_KEY
- Browser key(auto created by Firebase), Identity Toolkit API 제한 확인
```

### 조치 결과

```text
2019년 Browser key를 VITE_FIREBASE_API_KEY로 적용한 뒤 로그인 정상화
2026년 Browser key는 삭제하지 않고 사용 중지 또는 분리 검토 대상으로 판단
서비스 계정에 바인딩된 Gemini API key는 웹사이트 HTTP referrer 제한을 적용할 수 없음을 확인
```

---

## 3. 로그인 세션 유지 방식 보정

### 문제

로컬 서버를 재실행해도 로그인이 계속 유지되는 상태가 확인되었습니다.

### 수정 파일

```text
src/api/authApi.js
src/store/useAuthStore.js
src/App.jsx
```

### 수정 내용

```text
Firebase Auth persistence를 browserSessionPersistence로 설정
Zustand auth store에 Firebase Auth onAuthStateChanged 기반 동기화 추가
로컬 앱 세션 만료 시간을 별도로 관리
세션 만료 시 localStorage 상태와 Firebase Auth 상태를 함께 정리
앱 시작 시 Firebase Auth 실제 로그인 상태를 기준으로 사용자 상태 복구
```

### 기대 효과

```text
브라우저 세션 단위로 로그인 유지 범위를 제한
로컬 저장소만 남아 사용자가 로그인된 것처럼 보이는 상태 감소
Firebase Auth 실제 상태와 앱 전역 상태의 불일치 완화
```

---

## 4. AI 플래너 생성 방식 UI 재설계

### 기존 문제

기존 AI 플래너 화면은 위시리스트 후보가 개별 장소 목록으로 노출되어, 사용자가 만든 위시리스트 폴더를 기준으로 코스를 만드는 흐름인지 이해하기 어려웠습니다.

### 수정 파일

```text
src/pages/AiPlanner.jsx
```

### 수정 내용

AI 여행 코스 생성 방식을 두 가지 모드로 분리했습니다.

```text
설정한 조건으로 새 코스 생성하기
위시리스트 폴더 기반으로 코스 생성하기
```

#### 설정한 조건으로 새 코스 생성하기

```text
사용자가 입력한 지역, 일정, 동행, 예산, 이동 강도, 날씨, 스타일, 회피 조건을 기준으로 코스 생성
생성 전 관광공사 API에서 입력 지역 키워드로 후보 장소 12개 조회
조회된 TourAPI 후보 장소를 Gemini preferredPlaces로 전달
Gemini는 관광공사 등록 장소를 우선 사용하고, 부족할 경우에만 AI 생성 장소를 추가
저장 시 새 위시리스트 폴더 자동 생성
```

#### 위시리스트 폴더 기반으로 코스 생성하기

```text
사용자가 기존 위시리스트 폴더를 선택
선택한 폴더에 저장된 여행지 목록과 선택 개수를 표시
폴더 내 장소를 기본적으로 전체 선택
필요 시 장소별 포함/제외 가능
Gemini는 선택한 장소를 주요 방문지로 우선 배치
저장 시 기존 폴더에 AI 메모와 체크리스트만 추가
동일한 이름의 새 폴더를 다시 생성하지 않음
```

### UI 보정

```text
왼쪽 입력 패널 폭을 420px에서 500px로 확장
오른쪽 미리보기 패널은 남은 영역을 사용하도록 조정
생성 모드 버튼 텍스트가 2줄로 깨지지 않도록 whitespace-nowrap 적용
버튼 문구를 사용자가 이해하기 쉬운 문장형으로 변경
```

---

## 5. 입력 안내 문구 추가

### 지역 입력 안내

추가 문구:

```text
시/도, 시/군/구, 동네명까지 입력할 수 있습니다. 예: 부산, 해운대, 서울 종로
```

의도:

```text
사용자가 "부산", "해운대", "서울 종로"처럼 어느 수준까지 입력 가능한지 이해할 수 있게 함
```

### 예산 입력 안내

예산 기준을 1일 1인 기준으로 명확히 정리했습니다.

```text
낮음: 1일 1인 3만 원 이하, 무료/저가 관광지와 가성비 식사 중심
보통: 1일 1인 3만~8만 원, 일반 입장료·식사·카페 포함
높음: 1일 1인 8만 원 이상, 유료 전시·체험·분위기 좋은 식당/카페 포함
```

의도:

```text
전체 여행 예산인지, 1인 기준인지, 식사/카페/체험 포함 여부가 모호하지 않게 함
Gemini 프롬프트에도 동일한 예산 기준을 전달하여 응답 일관성 개선
```

---

## 6. Gemini 프롬프트 보강

### 수정 파일

```text
src/api/geminiApi.js
CodeTrip_Firebase/info/Gemini_프롬프트_설계.md
```

### 코드 반영 내용

Gemini 요청 프롬프트에 다음 정보를 추가했습니다.

```text
생성 방식: 조건 기반 새 코스 또는 위시리스트 폴더 기반
기준 폴더: 사용자가 선택한 위시리스트 폴더명
예산 기준: 낮음/보통/높음의 1일 1인 금액 기준
```

응답 규칙도 보강했습니다.

```text
preferredPlaces는 관광공사 API 또는 사용자의 위시리스트에 연결된 장소로 간주
가능한 한 preferredPlaces를 주요 방문지로 우선 사용
preferredPlaces의 contentid는 응답 contentId로 그대로 유지
조건 기반 새 코스에서 preferredPlaces가 있다면 부족할 때만 contentId가 null인 장소 추가
위시리스트 폴더 기반에서는 selected preferredPlaces를 주요 방문지로 우선 배치
필요한 식사/카페/보조 장소만 추가
```

### 기대 효과

```text
관광공사 API를 사용하는 이유가 결과 화면과 저장 구조에서 더 명확해짐
TourAPI contentId가 있는 장소가 더 많이 포함되어 위시리스트 카드 저장률 향상
AI가 임의로 만든 장소만 나열하는 문제 완화
```

---

## 6-1. 인원 수 및 총예산 프롬프트 반영

### 작업 배경

예산 설명을 `1일 1인 기준`으로 정리한 뒤, 실제 사용자가 2명 이상으로 여행할 경우 Gemini가 전체 예산 감각을 함께 고려하는지 점검했습니다.

확인 결과 기존 AI 플래너는 `동행 유형`만 가지고 있었고, 실제 `인원 수`를 별도로 입력하거나 총예산을 계산하는 로직은 없었습니다.

기존 전달 정보:

```text
동행 유형: 친구
예산 수준: 보통
예산 기준: 1일 1인 3만~8만 원
```

문제점:

```text
2명, 3명, 가족 단위 여행이어도 Gemini에는 실제 인원 수가 전달되지 않음
예산 기준은 1인 기준으로 명확하지만, 전체 여행 비용 감각은 모델이 추론해야 하는 상태
동행 유형과 실제 인원 수가 분리되어 있지 않아 예산 조정 근거가 부족함
```

### 수정 파일

```text
src/pages/AiPlanner.jsx
src/api/geminiApi.js
```

### UI 수정 내용

AI 플래너 입력 폼에 `People` 입력값을 추가했습니다.

```text
기본값: 2명
최소값: 1명
최대값: 10명
```

예산 안내 문구에는 1인 기준 금액과 함께 실제 인원 수를 곱한 예상 총예산 범위를 표시하도록 했습니다.

예시:

```text
Budget: 보통
People: 2

화면 안내:
1일 1인 3만~8만 원, 일반 입장료·식사·카페 포함
1일 총 6만 원~16만 원
```

### 프롬프트 수정 내용

Gemini 요청 프롬프트에 다음 항목을 추가했습니다.

```text
- 인원 수: 2명
- 예상 총예산 범위: 1일 총 6만 원~16만 원
```

응답 규칙에도 예산 판단 기준을 추가했습니다.

```text
예산은 1일 1인 기준과 예상 총예산 범위를 함께 고려하여 식사, 카페, 유료 체험 수준을 조절하세요.
```

### 예산 계산 기준

```text
낮음:
- 1일 1인 3만 원 이하
- 2명 기준 1일 총 6만 원 이하

보통:
- 1일 1인 3만~8만 원
- 2명 기준 1일 총 6만~16만 원

높음:
- 1일 1인 8만 원 이상
- 2명 기준 1일 총 16만 원 이상
```

### 기대 효과

```text
동행 유형과 실제 인원 수를 분리하여 더 현실적인 예산 판단 가능
1인 기준 예산 설명은 유지하면서 총예산 범위도 Gemini에 명시
가족/친구/연인 여행에서 식사, 카페, 체험 비용 수준을 더 자연스럽게 조정 가능
사용자 입장에서도 선택한 예산이 전체 인원 기준으로 어떻게 해석되는지 즉시 확인 가능
```

---

## 7. AI 코스 저장 구조 보정

### 기존 문제

AI 플래너에서 위시리스트 폴더 기반 코스를 생성한 뒤 저장하면, 사용자가 선택한 기존 폴더가 아니라 동일한 이름의 새 폴더가 다시 생성되었습니다.

### 수정 파일

```text
src/api/wishlistApi.js
src/pages/AiPlanner.jsx
```

### 수정 내용

`saveAiTripToFolder(plan)` 함수를 다음 구조로 변경했습니다.

```text
saveAiTripToFolder(plan, options)
options.folderId가 있으면 기존 폴더를 대상으로 저장
options.folderId가 없으면 새 폴더 생성
```

기존 폴더 저장 시:

```text
wishlistFolders/{folderId}/updated_at만 갱신
새 폴더를 생성하지 않음
AI 코스 요약 메모를 기존 폴더의 wishlistNotes에 MEMO로 추가
saveGuide.checklist 항목을 CHECKLIST note로 추가
TourAPI contentId가 있는 코스 장소는 기존 폴더의 wishlists 항목으로 추가 또는 이동
```

새 코스 저장 시:

```text
saveGuide.folderName 또는 plan.title 기준으로 새 wishlistFolders 생성
AI 코스 메모와 체크리스트 저장
contentId가 있는 장소는 위시리스트 카드로 저장
contentId가 없는 장소는 메모/체크리스트에만 남김
```

---

## 8. AI 생성 장소 표시 방식 논의

### 확인한 문제

관광공사 API에 등록되지 않은 AI 생성 장소는 `contentId`가 없기 때문에 기존 여행지 상세 페이지(`/explore/{contentId}`)로 연결할 수 없습니다.

이 장소를 억지로 위시리스트 카드처럼 저장하면 다음 문제가 생길 수 있습니다.

```text
VIEW_DATA 클릭 시 상세 페이지에 필요한 TourAPI detailCommon/detailIntro/detailImage 데이터를 조회할 수 없음
지도, 주소, 이미지, 입장료 등 신뢰 가능한 데이터가 부족함
TourAPI 장소 카드와 AI 생성 장소 카드의 정보 신뢰도가 섞임
```

### 제안 방향

AI 생성 장소를 기존 위시리스트 카드로 억지 저장하기보다, 별도 `AI 코스 기록` 또는 `AI Course Detail` 형태로 분리하는 방향을 제안했습니다.

권장 구조:

```text
TourAPI 장소
- 위시리스트 카드
- VIEW_DATA
- 관광공사 상세 정보 페이지 연결

AI 생성 코스
- AI 코스 기록 카드
- VIEW_COURSE
- title, summary, days, items, tips, checklist 기반의 게시글형 상세 화면 제공
```

후속 작업 후보:

```text
users/{uid}/aiCourses/{courseId}
users/{uid}/wishlistFolders/{folderId}/courseIds
AI 코스 상세 페이지 /mypage/course/{courseId}
폴더 화면에서 여행지 카드 영역과 AI 코스 기록 영역 분리
```

---

## 9. 마이페이지 표시 보정

### 문제

AI 코스 저장 후 폴더에 메모/체크리스트는 저장되었지만 여행지 카드가 0개일 경우, 사용자는 저장이 실패한 것으로 오해할 수 있었습니다.

### 수정 파일

```text
src/pages/MyPage.jsx
```

### 수정 내용

```text
Folder Notes에서 LIST와 MEMO 탭을 분리하여 각각 CHECKLIST와 MEMO만 표시
선택한 폴더에 여행지 카드는 없고 AI 메모/체크리스트만 있을 경우 안내 문구 표시
```

안내 문구:

```text
이 폴더에는 저장된 여행지 카드는 없고 AI 메모와 체크리스트만 저장되어 있습니다.
```

---

## 10. 검증 결과

다음 명령으로 앱 코드 정적 검증을 진행했습니다.

```bash
npx eslint src
```

결과:

```text
오류 0개
기존 React Hook warning 11개 유지
이번 작업으로 새 lint error는 발생하지 않음
```

기존 warning 범위:

```text
Header.jsx 알림 fetch effect
Board.jsx 게시글 fetch effect
BoardWrite.jsx useEffect dependency
Explore.jsx useEffect dependency
Festivals.jsx useEffect dependency
MyPage.jsx notes 초기화 effect
TravelDetail.jsx recently viewed/initWishlist dependency
TravelTagSearch.jsx fetch effect
```

---

## 11. 현재 상태

구현 완료:

```text
AI 플래너 생성 모드 분리
조건 기반 생성 시 관광공사 API 후보 장소 우선 조회
Gemini preferredPlaces에 TourAPI 후보 전달
위시리스트 폴더 기반 생성 시 기존 폴더 선택 및 장소 포함/제외
기존 폴더 저장 시 새 폴더 중복 생성 방지
지역/예산 입력 보조 설명 추가
예산 기준을 1일 1인 금액 기준으로 명확화
AI 코스 저장 결과 토스트 메시지 개선
메모/체크리스트 표시 탭 분리
Firebase Auth key 제한 문제 원인 정리
```

테스트 필요:

```text
조건 기반 새 코스 생성 시 TourAPI contentId 포함률 확인
위시리스트 폴더 기반 코스 저장 시 기존 폴더에만 메모/체크리스트가 추가되는지 확인
contentId가 있는 AI 코스 장소가 위시리스트 카드로 정상 저장되는지 확인
contentId가 없는 AI 생성 장소가 안내 문구와 메모로만 관리되는지 확인
```

---

## 12. 남은 작업

우선순위 높음:

```text
AI 생성 장소를 별도 AI 코스 기록 카드/상세 페이지로 분리할지 결정
조건 기반 생성 결과에서 관광공사 후보가 충분히 반영되는지 로컬 테스트
위시리스트 폴더 기반 저장 end-to-end 재테스트
```

우선순위 중간:

```text
TourAPI 후보 검색 품질 개선
지역명만으로 검색 결과가 부족할 경우 travelStyle 키워드 조합 검색 추가
AI 코스 메모를 긴 텍스트가 아닌 구조화된 코스 상세로 보여주는 UI 설계
```

제출 전:

```text
Gemini API 호출을 Firebase Functions + Secret 구조로 이전 검토
Firebase Blaze 전환 여부 결정
npm run lint, npm run build 재확인
Firebase Hosting 배포 후 실제 URL에서 Gemini/위시리스트 저장 흐름 확인
```

---

## 13. 비회원 메인 화면 UX 개편

### 작업 배경

기존 메인 화면은 로그인 여부와 관계없이 여행지 추천 대시보드 중심으로 구성되어 있었습니다.

비회원 사용자가 처음 서비스에 진입했을 때 CodeTrip이 어떤 흐름의 서비스인지 충분히 이해하기 어렵고, 회원가입 또는 로그인으로 이어지는 동기가 약해 보이는 문제가 있었습니다.

따라서 비회원에게는 서비스 소개형 홈 화면을 먼저 보여주고, 로그인한 사용자에게는 기존 위치/날씨 기반 대시보드를 유지하는 방향으로 구조를 분리했습니다.

### 주요 수정 파일

```text
src/pages/Home.jsx
src/components/Layout/Header.jsx
src/components/Layout/SideBar.jsx
src/main.jsx
public/favicon.svg
index.html
```

### 구현 내용

```text
비회원용 GuestHome 화면과 로그인 사용자용 기존 Home 대시보드 분리
비회원 상태에서는 위치, 날씨, 여행지 추천 API 호출을 실행하지 않도록 보정
여행 이미지 기반 히어로 영역 추가
히어로 문장의 강조 단어에 그라데이션 애니메이션 적용
메인 문장의 마침표를 프로젝트 메인 색상인 청록색으로 강조
스크롤 진입 시 콘텐츠가 자연스럽게 나타나는 reveal 애니메이션 적용
서비스 흐름을 탐색 -> 저장 -> 생성 순서로 보여주는 안내 섹션 추가
회원이 이용할 수 있는 위시리스트, AI 여행 코스, 게시판 기능 예시 카드 추가
하단 CTA 영역을 회원가입/로그인으로 이어지도록 재구성
```

### UI 방향

비회원 홈은 단순 설명 페이지가 아니라, 공모전 제출 시 첫 화면에서 서비스의 차별점을 빠르게 전달하는 목적의 화면으로 구성했습니다.

특히 다음 흐름이 보이도록 정리했습니다.

```text
1. 여행지를 먼저 둘러본다
2. 마음에 드는 장소를 저장한다
3. 저장한 장소나 입력 조건을 바탕으로 CodeTrip이 여행 코스를 생성한다
4. 로그인하면 위시리스트, AI 코스, 게시판 기록을 이어서 관리할 수 있다
```

---

## 14. 회원 전용 접근 안내 및 로그아웃 흐름 보정

### 작업 배경

기존에는 비회원 사용자가 회원 전용 페이지에 접근하거나 로그아웃을 시도할 때 브라우저 기본 `alert`, `confirm` 창이 노출되었습니다.

기본 브라우저 팝업은 현재 프로젝트의 UI 톤과 맞지 않고, 모바일 화면에서도 서비스 경험이 갑자기 끊기는 느낌이 있어 프로젝트 전용 모달로 교체했습니다.

### 주요 수정 파일

```text
src/components/ConfirmModal.jsx
src/components/ProtectedRoute.jsx
src/components/Layout/Header.jsx
src/components/Layout/SideBar.jsx
src/main.jsx
```

### 구현 내용

```text
프로젝트 톤에 맞춘 ConfirmModal 공통 컴포넌트 추가
로그아웃 확인창을 브라우저 confirm에서 ConfirmModal로 변경
로그아웃 완료 후 /login이 아닌 메인 화면 / 으로 이동하도록 변경
회원 전용 페이지 접근 시 기본 alert 대신 안내 모달 표시
ProtectedRoute 컴포넌트로 라우트 단위 접근 제한 추가
사이드바 메뉴 클릭 시 비회원 접근 제한 안내 처리
```

### 회원 전용으로 제한한 페이지

```text
/ai-planner
/board
/board/write
/board/tag-search
/board/:id
/mypage
/settings
/my-activity
```

게시판은 현재 회원 전용 접근으로 설정했습니다.

다만 추후 제출 직전 UX를 다시 검토할 때, 다음 두 방향 중 하나를 선택할 수 있습니다.

```text
1. 게시판 전체를 회원 전용으로 유지
2. 게시글 목록/상세는 비회원에게 공개하고 글쓰기, 댓글, 좋아요만 회원 전용으로 제한
```

공모전 제출용으로는 서비스 내용을 비회원도 조금 더 둘러볼 수 있는 2번 방식이 유리할 수 있으므로, 추후 개선 후보로 남깁니다.

---

## 15. 브랜드 UI 및 사용자 노출 용어 정리

### 작업 배경

Gemini API는 내부 구현 기술이지만, 사용자 화면에서 계속 `Gemini`라는 표현이 직접 노출되면 CodeTrip 자체 서비스가 아닌 외부 API 기능처럼 보일 수 있습니다.

따라서 사용자에게 보이는 문구는 `CodeTrip이 여행 코스를 생성한다`는 방향으로 통일하고, 내부 파일명이나 기술 문서에서는 Gemini 명칭을 유지했습니다.

### 구현 내용

```text
사용자 화면의 Gemini 표현을 CodeTrip 중심 문장으로 변경
Gemini API 오류 메시지를 사용자 친화적인 CodeTrip 코스 생성 실패 안내로 변경
사이드바 상단 브랜드명을 Code Trip에서 CodeTrip으로 통일
CodeTrip 전용 favicon.svg 제작 및 적용
좌측 상단 브랜드명 옆에 favicon 로고 표시
```

### 적용 기준

```text
사용자 화면: CodeTrip, AI 여행 코스, 여행 코스 생성
기술 구현: geminiApi.js, Gemini API, VITE_GEMINI_API_KEY
문서/트러블슈팅: Gemini API key, Google AI Studio, Firebase Functions 이전 계획
```

이 기준을 두어 사용자에게는 서비스 브랜드가 먼저 보이고, 개발 문서에서는 실제 기술 스택을 추적할 수 있도록 정리했습니다.

---

## 16. 검증 내용

실행한 검증:

```text
npx eslint src/pages/Home.jsx
npx eslint src
```

검증 결과:

```text
Home.jsx 단독 lint 통과
src 전체 lint 기준 신규 error 없음
기존 React Hook dependency 관련 warning 11건은 남아 있음
```

남아 있는 warning은 이번 홈 UX 및 접근 제한 작업에서 새로 만든 오류는 아니며, 기존 파일의 `useEffect`, `useCallback` 의존성 정리 이슈입니다.

추후 별도 안정화 작업에서 다음 파일들을 점검하면 됩니다.

```text
Header.jsx
Board.jsx
BoardWrite.jsx
Explore.jsx
Festivals.jsx
MyPage.jsx
TravelDetail.jsx
TravelTagSearch.jsx
```
