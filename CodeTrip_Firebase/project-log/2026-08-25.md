# 2026-08-25 작업 로그

> 이 문서는 PR #28 `feature/trip-date-planning` 브랜치에서 AI 플래너 폴더 주소 보강 경합 처리, Info 화면 버전 표기, 모바일 하단 내비게이션 보강, 개발용 캐시 패널 위치 조정 작업을 기록합니다.

## 1. 작업 개요

AI 여행 일정 날짜 기능을 추가한 이후, 폴더 기반 생성 흐름에서 비동기 주소 보강이 끝나기 전에 사용자가 생성 버튼을 누르거나 입력값을 변경할 수 있는 경로를 보완했습니다. 이번 작업의 목적은 폴더 선택, 장소 주소 보강, 지역명 자동 반영, 생성 요청이 서로 충돌하지 않도록 AI Planner의 busy-state와 최신성 검증을 더 명확하게 만드는 것입니다.

또한 제출 직전 MVP 후보 상태를 더 정확하게 드러내기 위해 Info 화면 버전 표기를 `MVP 0.9.0-rc`로 조정했습니다. 모바일 사용성 측면에서는 하단 내비게이션에 `Info` 진입점을 추가하고, `My Page`는 사용자 관점에서 가장 찾기 쉬운 오른쪽 끝에 유지하도록 정리했습니다. `Info` 메뉴는 데스크톱과 동일하게 외부 참고 링크와 About 화면으로 이어지는 하위 메뉴를 모바일 팝업으로 제공합니다.

## 2. AI Planner 폴더 주소 보강 상태 처리

폴더 기반 AI 일정 생성 모드에서는 선택한 폴더 안의 여행지 주소가 부족한 경우 `hydratePlaceAddresses()`가 비동기로 주소를 보강합니다. 이 작업이 진행 중일 때 생성 버튼이 활성화되어 있으면, 화면에는 아직 보강 중인 데이터가 표시되고 있는데 생성 요청은 이전 상태를 기준으로 실행될 수 있습니다.

이를 보완하기 위해 `folderHydrating` 상태를 추가했습니다.

- 폴더 장소 주소 보강 시작 시 `folderHydrating`을 `true`로 설정합니다.
- `plannerBusy` 조건에 `generating`, `saving`, `folderHydrating`을 함께 포함합니다.
- 생성 또는 저장 중이 아닌 경우에만 폴더 변경을 허용하도록 `plannerActionBusy`를 분리했습니다.
- 주소 보강 결과는 `folderSelectionRequestRef`와 `plannerRevisionRef`가 모두 최신일 때만 화면 상태에 반영합니다.
- 주소 보강 실패 시에는 기존 폴더 장소 목록을 fallback으로 사용하고, 사용자가 생성 흐름을 이어갈 수 있도록 했습니다.

이 변경으로 폴더 선택 직후 주소 보강이 진행되는 짧은 구간에서 중복 생성, 오래된 폴더 결과 반영, 지역명 덮어쓰기 가능성을 줄였습니다.

## 3. Info 화면 버전과 서비스명 표기 정리

Info 화면의 시스템 표기는 최종 정식 배포가 아닌 공모전 제출 전 MVP 후보 상태를 나타내도록 `system.info — MVP 0.9.0-rc`로 조정했습니다.

서비스명도 화면 내 표기를 `CodeTrip`으로 맞췄습니다. 이는 README와 기능설명서, 제출 문서에서 사용하는 서비스명과 UI 표기를 일치시키기 위한 정리입니다.

## 4. 모바일 하단 내비게이션 Info 진입점 추가

모바일 웹 화면에서는 하단 탭 영역에 `Info` 메뉴가 노출되지 않아 정보 화면 접근성이 떨어지는 문제가 있었습니다. 사용자 관점에서는 개인 계정과 저장 데이터를 확인하는 `My Page`가 가장 오른쪽에 고정되는 편이 더 자연스럽기 때문에, 모바일 하단 내비게이션 순서를 아래처럼 정리했습니다.

1. Home
2. Explore
3. Festivals
4. AI Planner
5. Board
6. Info
7. My Page

이 구조는 `My Page`를 오른쪽 끝에 유지하면서도, 서비스 정보와 시스템 안내 화면에 모바일에서도 접근할 수 있게 합니다.

`Info` 탭은 단순히 `/info`로 바로 이동하는 링크가 아니라, `My Page`와 같은 방식의 모바일 하위 메뉴 팝업을 띄우도록 수정했습니다. 모바일 팝업에서는 공공 와이파이, 세이프스테이, 교통 예약 링크와 About CodeTrip 화면으로 이동할 수 있습니다. 다른 하단 메뉴를 누르면 `Info` 팝업과 `My Page` 팝업이 서로 닫히도록 처리했습니다.

## 5. Info 전용 개발용 API 캐시 패널 위치 조정

개발 모드에서 표시되는 `API_CACHE.LOG` 패널이 화면 폭을 줄였을 때 왼쪽 사이드바 또는 메뉴 항목과 겹치는 문제가 있었습니다. 이 패널은 사용자 기능이 아니라 개발·검증용 보조 UI이므로, Info 화면에서만 표시되도록 제한하고 주요 내비게이션과 충돌하지 않도록 오른쪽 하단 기준으로 위치를 조정했습니다.

이 변경은 API 캐시 상태를 확인하는 기능 자체를 바꾸지 않고, 일반 탐색·축제·AI Planner 화면의 콘텐츠를 가리지 않도록 표시 범위를 줄이는 UI 안정화 작업입니다.

## 6. 검증 결과

- `npm run lint`
  - 통과했습니다.
  - 신규 오류는 없었습니다.
  - 기존 React Hook 관련 warning 11개는 유지됐습니다.

- `npm run build`
  - 통과했습니다.
  - 기존 Vite 500kB 초과 청크 경고는 유지됐습니다.

- 정적 확인
  - AI Planner 변경 범위는 폴더 선택과 주소 보강 상태 처리에 한정했습니다.
  - 모바일 하단 내비게이션은 `Info` 탭 추가와 `My Page` 우측 고정에 한정했습니다.
  - `.codex-tmp/`, `output/`은 커밋 대상에서 제외합니다.

- 수동 확인
  - `/info` 화면에서는 `API_CACHE.LOG` 개발용 캐시 카드가 정상 표시되는 것을 확인했습니다.
  - `/explore` 화면에서는 `API_CACHE.LOG` 개발용 캐시 카드가 더 이상 표시되지 않는 것을 확인했습니다.

## 7. 관련 커밋

- `0ae2017 260825 fix: AI 플래너 폴더 주소 보강 상태 처리`

## 8. 후속 작업

- 모바일 실제 화면 또는 DevTools 모바일 폭에서 하단 탭 7개가 과도하게 겹치지 않는지 확인합니다.
- 모바일 실제 화면에서 `Info` 하위 메뉴 팝업이 예상 위치에 표시되는지 확인합니다.
- PR #28의 남은 CodeRabbit 피드백이 있다면 별도 커밋으로 반영합니다.
- 최종 제출 전에는 기능설명서와 배포 사이트의 기능 설명이 현재 구현 상태와 일치하는지 다시 확인합니다.

## 9. TourAPI 신규 여행지 알림 구조 추가

AWS/Express 환경에서 사용하던 한국관광공사 신규 여행지 알림 아이디어를 현재 Firebase 구조에서도 적용할 수 있는지 검토하고, Firebase 기반 MVP 구현을 추가했습니다.

이번 구현은 사용자별 알림을 대량 생성하는 방식이 아니라, 서버 배치가 공용 신규 여행지 피드를 저장하고 Header 알림 UI가 기존 개인 알림과 함께 표시하는 방식입니다. 이 구조는 사용자 수가 늘어도 Realtime Database 쓰기량이 급격히 증가하지 않아 비용과 운영 안정성 측면에서 더 적합합니다.

### 9.1 Functions 구조

- `functions/index.js`에 `syncTourApiUpdates` Scheduled Function을 추가했습니다.
- 함수는 하루 1회 `KorService2.areaBasedList2`를 조회합니다.
- 최신 30개 항목을 확인하고, 기존에 저장되지 않은 `contentId`만 신규 여행지로 저장합니다.
- 신규 여행지는 `tourApiUpdates/items/{contentId}`에 저장합니다.
- 저장된 업데이트 피드는 최근 100개까지만 유지하도록 정리 기준을 추가했습니다.
- TourAPI 인증키는 `TOUR_API_SERVICE_KEY` Functions Secret으로 관리하도록 설계했습니다.

### 9.2 알림 UI 연동

- `src/api/notificationApi.js`에서 기존 `users/{uid}/notifications` 알림과 `tourApiUpdates/items` 공용 신규 여행지 알림을 병합합니다.
- 신규 여행지 알림은 `[신규 여행지] {title}` 형식의 메시지로 표시합니다.
- 사용자가 알림을 클릭하면 `/explore/{contentId}` 상세 화면으로 이동합니다.
- 읽음, 개별 숨김, 읽은 알림 삭제 상태는 `users/{uid}/tourApiUpdateReads/{contentId}`에 저장합니다.
- 공용 업데이트 피드를 사용자별로 복제하지 않고, 사용자별 상태만 분리해 저장합니다.

### 9.3 데이터·보안 규칙

- `database.rules.json`에 `tourApiUpdates` 읽기 규칙을 추가했습니다.
- `tourApiUpdates` 클라이언트 쓰기는 차단했습니다.
- `users/{uid}/tourApiUpdateReads`는 본인 경로에서만 읽음·숨김 상태를 저장할 수 있도록 기존 `users/{uid}` 권한 경계 안에 배치했습니다.

### 9.4 검증 결과

- `npm run lint`
  - 통과했습니다.
  - 신규 오류는 없었습니다.
  - 기존 React Hook 관련 warning 11개는 유지됐습니다.

- `npm run build`
  - 통과했습니다.
  - 기존 Vite 500kB 초과 청크 경고는 유지됐습니다.

- `npm --prefix functions run lint`
  - 통과했습니다.

### 9.5 배포 전 준비 사항

Functions 배포 전에 아래 Secret을 등록해야 합니다.

```bash
firebase functions:secrets:set TOUR_API_SERVICE_KEY
```

Secret에는 한국관광공사 TourAPI 서비스 키를 입력합니다. 인증키는 저장소, 문서, 로그에 직접 기록하지 않습니다.

### 9.6 후속 확인 사항

- `syncTourApiUpdates` 배포 후 Firebase Console 또는 Functions 로그에서 정상 실행 여부를 확인합니다.
- `tourApiUpdates/items`에 신규 항목이 저장되는지 확인합니다.
- 로그인 상태의 Header 알림 목록에 신규 여행지 알림이 표시되는지 확인합니다.
- 알림 클릭 시 여행지 상세 화면으로 이동하는지 확인합니다.
- 개인화 알림은 추후 관심 지역, 위시리스트 폴더, 최근 탐색 지역 기준으로 확장할 수 있습니다.

## 10. TourAPI 신규 여행지 알림 CodeRabbit 피드백 반영

PR #29에서 확인된 TourAPI 신규 여행지 알림 관련 피드백을 반영했습니다.

### 10.1 반영 내용

- `syncTourApiUpdates`의 TourAPI 조회 정렬 기준을 이미지 포함 등록일 최신순으로 변경했습니다.
  - 기존 `arrange=O`는 신규 등록 감지 목적에 적합하지 않을 수 있어 `arrange=R`로 조정했습니다.
- TourAPI 응답 검증을 강화했습니다.
  - `response.header.resultCode`가 `0000`이 아닌 경우 동기화 실패로 처리합니다.
  - `response.body.items.item` 구조가 없으면 성공한 빈 결과로 기록하지 않고 오류로 처리합니다.
- Header 알림 병합 로직에서 TourAPI 신규 여행지 알림의 숨김 필터 순서를 조정했습니다.
  - 숨김 상태를 먼저 제거한 뒤 표시 개수 제한을 적용하도록 변경했습니다.
- `docs/39-tourapi-update-notification-plan.md`에 신규 알림 동기화 경로의 Secret 기준, 응답 검증 기준, 등록일 최신순 조회 기준을 보강했습니다.

### 10.2 검증 결과

- `npm --prefix functions run lint`
  - 통과했습니다.

- `npm run lint`
  - 통과했습니다.
  - 기존 React Hook 관련 warning 11개는 유지됐습니다.

- `npm run build`
  - 통과했습니다.
  - 기존 Vite 500kB 초과 청크 경고는 유지됐습니다.

### 10.3 후속 확인 사항

- 스케줄 실행 시간이 지난 뒤 Firebase Functions 로그에서 `syncTourApiUpdates` 실행 결과를 확인합니다.
- Realtime Database에 `tourApiUpdates/items`, `tourApiUpdates/state` 경로가 생성되는지 확인합니다.
- 신규 여행지 알림이 Header 알림 목록에 표시되고, 숨김 처리 후에도 표시 개수 제한이 의도대로 적용되는지 확인합니다.

## 11. 문서 작성 규칙 보강

작업 내역이 코드 변경에 비해 늦게 문서화되는 것을 방지하기 위해 문서 작성 규칙을 보강했습니다.

### 11.1 변경 방향

- 앞으로 코드 수정, 기능 추가, 배포 설정 변경, 보안 규칙 변경, CodeRabbit 피드백 반영처럼 프로젝트 상태가 바뀌는 작업은 커밋 생성 전에 날짜별 작업 로그를 먼저 작성합니다.
- 원인 분석이나 장애 대응이 포함된 경우에는 날짜별 로그뿐 아니라 `TROUBLESHOOTING.md` 색인도 함께 갱신합니다.
- 전체 작업 요약이 바뀌는 경우에는 `PROJECT_LOG.md` 한 줄 요약도 함께 갱신합니다.

### 11.2 반영 문서

- `CodeTrip_Firebase/guides/Document_Guide.md`
  - `작업 수행 → 날짜별 로그 작성 → 필요한 경우 트러블슈팅 색인 갱신 → PROJECT_LOG 요약 확인 → 검증 → 커밋` 순서를 커밋 전 확인 기준으로 추가했습니다.
- `CodeTrip_Firebase/TROUBLESHOOTING.md`
  - TourAPI 스케줄 함수 배포 후 DB 경로가 즉시 보이지 않는 상태를 신규 트러블슈팅 색인으로 추가했습니다.
  - TourAPI 실패 응답을 성공한 빈 결과로 기록할 수 있는 문제를 신규 트러블슈팅 색인으로 추가했습니다.
- `CodeTrip_Firebase/PROJECT_LOG.md`
  - 2026-08-25 작업 요약에 PR #29 CodeRabbit 피드백 반영과 트러블슈팅 색인 보강 내용을 추가했습니다.
  - 작성 기준에 커밋 생성 전 문서 갱신 규칙을 추가했습니다.

### 11.3 적용 기준

이후 작업부터는 커밋을 만들기 전에 아래 순서를 확인합니다.

1. 오늘 작업 로그에 변경 배경, 수정 내용, 검증 결과를 작성합니다.
2. 장애·오류·외부 API·배포·보안·비동기 상태 이슈가 있으면 `TROUBLESHOOTING.md`에 색인을 추가합니다.
3. 날짜별 대표 요약이 바뀌면 `PROJECT_LOG.md`도 갱신합니다.
4. 검증 명령 결과를 확인한 뒤 커밋을 생성합니다.

## 12. 프로필·게시글 이미지 Firebase Storage 업로드 전환

프로필 이미지 입력을 URL 또는 base64 data URL에 의존하던 구조를 Firebase Storage 업로드 후 다운로드 URL을 저장하는 구조로 전환했습니다. 같은 방식으로 게시글 작성 화면에서도 로컬 이미지 파일을 업로드해 Markdown 이미지 링크로 삽입할 수 있도록 확장했습니다.

### 12.1 배경

- 프로필 수정 화면에서 데스크톱 이미지 파일을 선택하면 기존 `authApi.uploadImage()`가 파일을 base64 data URL로 변환했습니다.
- base64 data URL은 길이가 매우 길어 Firebase Authentication `photoURL` 제한을 초과할 수 있고, 실제 프로필 저장 시 `auth/invalid-profile-attribute` 오류가 발생했습니다.
- 게시글 작성 화면은 이미지 URL 직접 입력 중심이라 로컬 파일 첨부가 어렵고, 긴 data URL을 본문에 넣는 방식은 문서 크기와 렌더링 안정성 측면에서 적합하지 않았습니다.

### 12.2 변경 내용

- `src/firebase.js`
  - Firebase Storage 인스턴스를 초기화하고 `firebaseStorage`로 export했습니다.
- `src/api/storageApi.js`
  - 공통 이미지 업로드 API를 추가했습니다.
  - 인증 사용자 확인, 이미지 타입 검증, 1MB 초과 이미지 클라이언트 압축, 파일명 정규화, Storage 업로드, 다운로드 URL 반환을 담당합니다.
  - 프로필 이미지는 `users/{uid}/profile`, 게시글 이미지는 `users/{uid}/board` 경로에 저장합니다.
- `src/api/authApi.js`
  - 프로필 이미지 업로드를 FileReader data URL 변환에서 Firebase Storage 업로드로 변경했습니다.
  - 프로필 저장 시에는 짧은 다운로드 URL을 `photoURL`과 사용자 프로필 데이터에 저장합니다.
- `src/pages/Settings.jsx`
  - 프로필 이미지 업로드 안내 문구를 Storage 업로드 기준으로 변경했습니다.
  - 사용자는 파일 업로드 또는 외부 이미지 URL 입력을 선택할 수 있습니다.
- `src/components/MarkdownEditor.jsx`, `src/pages/BoardWrite.jsx`
  - Markdown 에디터에 이미지 파일 업로드 버튼을 추가했습니다.
  - 게시글 작성 중 파일을 선택하면 Storage에 업로드하고, 반환된 다운로드 URL을 `![이미지명](URL)` 형식으로 본문에 삽입합니다.
- `firebase.json`, `storage.rules`
  - Firebase Storage Rules 배포 대상을 추가했습니다.
  - Storage Rules는 `users/{uid}/profile`과 `users/{uid}/board` 하위 경로에서 인증된 동일 UID만 이미지 파일을 쓸 수 있도록 제한합니다.

### 12.3 보안 기준

- 이미지 원본 또는 base64 data URL을 Realtime Database에 저장하지 않습니다.
- Storage 쓰기는 인증된 사용자 본인의 UID 경로로 제한합니다.
- 허용 폴더는 `profile`, `board`로 제한합니다.
- 업로드 파일은 이미지 MIME 타입이어야 하며, Rules 기준 2MB 미만이어야 합니다.
- 프로필과 게시글 이미지는 화면 렌더링을 위해 다운로드 URL을 공개 읽기 가능한 자원으로 취급합니다.

### 12.4 검증 결과

- `npm run lint`
  - 통과했습니다.
  - 기존 React Hook warning 11개는 유지됐습니다.
- `npm run build`
  - 통과했습니다.
  - 기존 Vite 500kB 초과 청크 경고는 유지됐습니다.
- Storage Rules 배포 후 수동 확인
  - 프로필 수정 화면에서 로컬 이미지 파일을 업로드하고 저장합니다.
  - 저장 후 Header, SideBar, 마이페이지 프로필 이미지가 Storage 다운로드 URL 기준으로 표시되는지 확인합니다.
  - 게시글 작성 화면에서 이미지 파일을 첨부하고 Markdown 본문에 이미지 링크가 삽입되는지 확인합니다.
  - 게시글 상세 화면에서 첨부 이미지가 정상 표시되는지 확인합니다.
  - 비로그인 또는 다른 UID가 Storage 쓰기를 수행할 수 없는지 규칙 검증을 추가로 진행합니다.
  - 이 수동 확인은 Storage Rules와 Hosting 배포 후 진행합니다.

### 12.5 후속 작업

- Firebase Storage Rules를 배포해야 실제 배포 환경에서 업로드 권한이 적용됩니다.
- Hosting 재배포 후 배포 사이트에서 프로필 이미지 업로드와 게시글 이미지 업로드를 수동 검증합니다.
- 필요하면 게시글 이미지 업로드 파일 크기 제한과 압축 품질을 별도 UX 메시지로 세분화합니다.

### 12.6 프로필 이미지 저장 경로 보정

- 프로필 이미지를 변경할 때마다 `users/{uid}/profile/{timestamp}-파일명` 경로에 새 파일이 누적 저장되는 구조를 확인했습니다.
- 프로필 이미지는 사용자가 현재 사진 1개만 필요로 하는 성격이 강하므로, 불필요한 Storage 파일 누적을 줄이기 위해 고정 경로 덮어쓰기 방식으로 변경했습니다.
- 변경 후 프로필 이미지는 `users/{uid}/profile/avatar` 경로에 업로드됩니다.
- 같은 Storage 객체를 덮어쓰면 다운로드 URL이 같아 브라우저가 이전 이미지를 캐시할 수 있으므로, 프로필 저장 URL에는 `v={uploadedAt}` 쿼리 파라미터를 붙여 최신 이미지 표시를 유도합니다.
- 게시글 이미지는 게시글 본문 Markdown이 과거 이미지 URL을 계속 참조할 수 있으므로 기존처럼 `users/{uid}/board/{timestamp}-파일명` 누적 저장 방식을 유지합니다.

## 13. PR #29 CodeRabbit 추가 피드백 반영

Firebase Storage 생성과 배포 이후 CodeRabbit이 남긴 추가 피드백을 반영했습니다. 이번 수정은 TourAPI 신규 여행지 동기화의 정상 빈 응답 처리, Storage 이미지 압축 기준, TourAPI 알림 일괄 처리 범위, Firebase 배포 가이드 정합성을 보강하는 작업입니다.

### 13.1 TourAPI 정상 빈 응답 처리

- `syncTourApiUpdates`에서 사용하는 TourAPI 응답 파싱 로직을 `functions/tourApiUpdates.js`로 분리했습니다.
- 한국관광공사 TourAPI가 `resultCode: "0000"`, `totalCount: "0"`, `items: ""` 형태로 정상 빈 결과를 반환하는 경우 동기화 실패가 아닌 빈 배열로 처리하도록 수정했습니다.
- `totalCount`가 0이 아닌데 `items.item` 구조가 없으면 기존처럼 응답 구조 오류로 분류합니다.
- `functions/test/tourApiUpdates.test.js`를 추가해 정상 빈 응답, 비정상 누락 구조, 단일 item 정규화 fixture를 검증합니다.

### 13.2 Storage 이미지 압축 기준 보강

- 기존에는 파일 크기가 1MB 이하이면 이미지 해상도를 확인하지 않고 그대로 반환했습니다.
- 변경 후에는 모든 이미지 파일을 로드해 `naturalWidth`, `naturalHeight`를 먼저 확인합니다.
- 파일 용량이 1MB 이하더라도 가로 또는 세로가 1920px을 초과하면 1920px 이하로 리사이즈한 뒤 업로드합니다.
- 파일 용량과 해상도가 모두 기준 이하면 원본 파일을 유지합니다.

### 13.3 TourAPI 알림 일괄 처리 범위 보정

- Header 표시용 TourAPI 신규 여행지 알림은 기존처럼 최신 10개만 표시합니다.
- `markAllRead()`와 `deleteReadNotifications()`는 표시 제한 10개에 묶이지 않고 숨김 처리되지 않은 전체 TourAPI 신규 여행지 알림을 대상으로 처리하도록 분리했습니다.
- 이 변경으로 오래된 TourAPI 알림도 일괄 읽음 또는 읽은 알림 삭제 동작에서 누락되지 않도록 했습니다.

### 13.4 Firebase 배포 가이드 정합성 보강

- `CodeTrip_Firebase/guides/Project_Firebase_배포.md`의 `firebase.json` 예시에 `storage.rules` 배포 설정을 추가했습니다.
- Storage를 사용하는 현재 구조에 맞춰 Hosting, Realtime Database Rules, Storage Rules가 함께 관리된다는 설명으로 수정했습니다.

### 13.5 검증 계획

- `npm --prefix functions run lint`
  - 통과했습니다.

- `npm --prefix functions test`
  - 통과했습니다.
  - 정상 빈 응답, 비정상 누락 구조, 단일 item 정규화 fixture 3건이 모두 성공했습니다.

- `npm run lint`
  - 통과했습니다.
  - 기존 React Hook warning 11개는 유지됐습니다.

- `npm run build`
  - 통과했습니다.
  - 기존 Vite 500kB 초과 청크 경고와 plugin timing warning은 유지됐습니다.

## 14. PR #29 CodeRabbit 추가 피드백 2차 반영

CodeRabbit이 추가로 남긴 Storage 이미지 압축 실패 처리와 TourAPI 알림 일괄 삭제 범위 피드백을 확인하고 반영했습니다. 이번 수정은 사용자가 업로드하는 이미지가 검증 기준을 우회하지 못하게 하고, 읽은 TourAPI 알림 삭제가 화면에 표시된 최신 10개에만 제한되지 않도록 하는 안정성 보완 작업입니다.

### 14.1 Storage 이미지 압축 실패 시 업로드 차단

- 기존 압축 로직은 Canvas context 생성 실패, `toBlob()` 실패, 이미지 로드 실패 상황에서 원본 파일을 그대로 반환할 수 있었습니다.
- 이 경우 원본 파일이 1MB를 초과하거나 1920px 제한을 초과해도 업로드까지 이어질 가능성이 있었습니다.
- 변경 후에는 다음 상황을 업로드 실패로 처리합니다.
  - 브라우저가 이미지 파일을 읽지 못하는 경우
  - Canvas context를 생성하지 못하는 경우
  - JPEG Blob 생성에 실패하는 경우
  - 압축 품질을 낮춰도 1MB 이하로 줄어들지 않는 경우
- 따라서 `uploadBytes()`로 전달되는 파일은 원본 유지 조건을 만족하거나, 압축 결과가 1MB 이하인 경우로 제한됩니다.

### 14.2 읽은 TourAPI 알림 삭제 범위 보정

- `deleteReadNotifications()`가 표시용 기본 제한인 최신 10개 TourAPI 알림만 조회하는 문제를 확인했습니다.
- 변경 후에는 `getTourApiUpdateNotifications(user.id, { limit: null })`을 사용해 숨김 처리되지 않은 전체 TourAPI 알림을 대상으로 읽은 알림 숨김 처리를 수행합니다.
- `markAllRead()`와 동일한 범위 기준을 사용하므로, 11번째 이후의 읽은 TourAPI 알림도 “읽은 알림 삭제” 동작에서 누락되지 않습니다.

### 14.3 검증 결과

- `npm run lint`
  - 통과했습니다.
  - 기존 React Hook warning 11개는 유지됐습니다.
- `npm run build`
  - 통과했습니다.
  - sandbox 권한 제한으로 최초 실행 시 Vite 임시 파일 생성 오류가 발생했으나, 권한 상승 재실행 후 production build가 정상 완료됐습니다.
  - 기존 Vite 500kB 초과 청크 경고와 plugin timing warning은 유지됐습니다.
- 배포 후에는 다음 수동 검증을 진행합니다.
  - 1MB 초과 이미지를 프로필 또는 게시글 이미지로 업로드했을 때 압축 또는 실패 안내가 정상 동작하는지 확인합니다.
  - 읽은 TourAPI 알림이 10개를 초과하는 상태에서 읽은 알림 삭제 시 오래된 읽은 알림도 숨김 처리되는지 확인합니다.

## 15. Home 회원 작업공간 카드 반응형 보정

메인 화면을 좁은 웹 폭 또는 모바일 웹 폭으로 줄였을 때 `member.workspace` 영역의 카드 3개가 한 줄에 고정되어 텍스트가 세로로 깨지는 문제를 확인했습니다.

### 15.1 원인

- 카드 목록이 `sm:grid-cols-3` 기준으로 고정되어 있었습니다.
- 화면 전체 폭은 Tailwind `sm` 이상으로 판단되지만, 사이드바나 브라우저 축소 상태 때문에 실제 콘텐츠 영역은 카드 3개를 담기 어려운 구간이 있었습니다.
- 카드 내부 배지와 제목, 설명 텍스트가 좁은 카드 폭 안에서 강제로 줄바꿈되어 가독성이 크게 떨어졌습니다.

### 15.2 수정 방향

- 카드 그리드를 고정 3열에서 `repeat(auto-fit, minmax(150px, 1fr))` 구조로 변경했습니다.
- 콘텐츠 영역이 좁으면 카드가 2열 또는 1열로 자연스럽게 내려가도록 했습니다.
- 카드 내부 배지는 `flex-wrap`, `truncate`, `min-w-0` 조합으로 좁은 폭에서도 레이아웃을 밀어내지 않도록 보정했습니다.
- 제목의 마지막 문구는 좁은 폭에서 별도 줄로 내려가도록 `block sm:inline` 기준을 추가했습니다.

### 15.3 검증 결과

- 모바일 웹 폭에서 `member.workspace` 카드가 겹치거나 세로로 찢어지지 않는지 확인합니다.
- 데스크톱 폭에서는 기존처럼 3개 카드가 한 줄에 자연스럽게 표시되는지 확인합니다.
- `npm run lint`
  - 통과했습니다.
  - 기존 React Hook warning 11개는 유지됐습니다.
- `npm run build`
  - 통과했습니다.
  - 기존 Vite 500kB 초과 청크 경고와 plugin timing warning은 유지됐습니다.

## 16. Home 회원 작업공간 카드 2차 레이아웃 보정

1차 보정 후 텍스트가 세로로 찢어지는 문제는 해소됐지만, 일부 웹 축소 폭에서 왼쪽 요약 카드가 세로로 길게 쌓이고 오른쪽 하단 영역에 큰 빈 공간이 남는 문제를 추가로 확인했습니다.

### 16.1 원인

- `member.workspace` 섹션이 `lg` 구간부터 왼쪽 요약 카드 영역과 오른쪽 저장 코스 미리보기 영역을 2열로 배치했습니다.
- 사이드바가 보이는 중간 폭에서는 전체 뷰포트가 `lg` 이상이어도 실제 콘텐츠 영역이 좁아져 왼쪽 카드가 한 줄로 배치되지 못했습니다.
- 오른쪽 미리보기 카드는 520px 고정 열을 차지하므로, 왼쪽에는 세로 카드만 쌓이고 오른쪽 하단은 빈 공간으로 남았습니다.

### 16.2 수정 방향

- 좌우 2열 전환 기준을 `lg`에서 `xl`로 늦춰 중간 폭에서는 오른쪽 저장 코스 미리보기가 아래로 내려가도록 했습니다.
- 왼쪽 요약 카드 3개는 `repeat(auto-fit, minmax(170px, 1fr))` 기준으로 compact하게 배치되도록 조정했습니다.
- 카드 최소 높이를 210px에서 180px로 낮추고, 내부 여백을 좁은 폭에서는 4단계로 줄여 화면 하단의 빈 공간을 줄였습니다.

### 16.3 검증 계획

- 웹 창을 사이드바가 보이는 중간 폭으로 줄였을 때 왼쪽 요약 카드가 불필요하게 길게 늘어나지 않는지 확인합니다.
- 오른쪽 저장 코스 미리보기 카드가 중간 폭에서는 아래로 내려가고, 넓은 데스크톱 폭에서는 다시 오른쪽 열로 배치되는지 확인합니다.
- 모바일 폭에서 카드 텍스트가 다시 세로로 찢어지지 않는지 확인합니다.

## 17. 모바일 하단 메뉴와 푸터 레이아웃 보정

iPhone 12 Pro 기준 모바일 웹 검증 중 하단 메뉴의 `AI Planner` 라벨이 좁은 폭에서 깨지고, 푸터의 시스템 상태 문구와 외부 링크가 한 줄에 함께 배치되면서 푸터 높이가 불필요하게 길어지는 문제를 확인했습니다.

### 17.1 원인

- 모바일 하단 메뉴가 7개 항목을 동일 폭으로 나누는 구조라 `AI Planner`처럼 긴 라벨은 390px 폭에서 줄바꿈될 수 있었습니다.
- 푸터는 시스템 상태 문구와 `PUBLIC_WIFI`, `SAFESTAY`, `INFO` 링크를 한 줄 flex로 배치하고 있어, 모바일 폭에서 주석 문구가 여러 줄로 깨졌습니다.

### 17.2 수정 방향

- 모바일 하단 메뉴에서 `AI Planner` 항목은 화면 폭을 고려해 `AI` 라벨로 축약해 표시하도록 했습니다.
- 하단 메뉴 아이콘 크기와 라벨 크기, 간격을 소폭 줄여 iPhone 12 Pro 폭에서 메뉴가 안정적으로 표시되도록 조정했습니다.
- 푸터는 모바일에서 시스템 상태 문구를 한 줄 단독 영역으로 배치하고, 하단에 `PUBLIC_WIFI`, `SAFESTAY`, `INFO` 링크를 별도 줄로 배치하도록 변경했습니다.
- 데스크톱에서는 기존처럼 시스템 상태 문구와 링크가 좌우로 배치되도록 유지했습니다.

### 17.3 검증 계획

- iPhone 12 Pro 크기에서 하단 메뉴의 `AI` 라벨과 아이콘이 겹치거나 줄바꿈되지 않는지 확인합니다.
- 모바일 푸터에서 `/* © 2026 CodeTrip - System Status: Optimal */` 문구가 한 줄로 표시되는지 확인합니다.
- 모바일 푸터 링크가 시스템 상태 문구 아래에서 `PUBLIC_WIFI`, `SAFESTAY`, `INFO` 순서로 표시되는지 확인합니다.

## 18. My Page 모바일 폴더 목록 접힘 UI 적용

iPhone 12 Pro 기준으로 마이페이지의 폴더 수가 많아질 경우 좌측 `FOLDERS` 패널이 길게 늘어나 핵심 콘텐츠까지 도달하기 위해 과도한 스크롤이 필요한 문제를 확인했습니다.

### 18.1 원인

- 데스크톱 기준의 폴더 전체 목록이 모바일에서도 그대로 노출됐습니다.
- 폴더가 20개 이상으로 늘어난 상태에서 `ALL_PLACES`, `UNCATEGORIZED`, 사용자 폴더 전체가 세로로 나열되어 화면 대부분을 차지했습니다.
- 선택된 폴더를 확인하기 위해 전체 목록을 항상 보여줄 필요는 없지만, 모바일에서는 접힘 상태가 없었습니다.

### 18.2 수정 방향

- 데스크톱에서는 기존처럼 폴더 목록을 항상 표시합니다.
- 모바일에서는 `selected_folder` 요약 버튼을 먼저 표시하고, 폴더 목록은 기본 접힘 상태로 전환했습니다.
- 사용자가 `FOLDERS` 헤더의 펼침 버튼 또는 `selected_folder` 버튼을 누르면 목록이 펼쳐집니다.
- 펼친 목록은 최대 높이 320px로 제한하고, 초과 항목은 폴더 영역 내부에서만 스크롤되도록 했습니다.
- 폴더를 선택하면 모바일 목록이 자동으로 접혀 선택 결과와 본문 콘텐츠가 더 빨리 보이도록 했습니다.

### 18.3 검증 계획

- iPhone 12 Pro 크기에서 마이페이지 진입 시 폴더 전체 목록이 기본으로 길게 노출되지 않는지 확인합니다.
- `selected_folder` 버튼을 누르면 폴더 목록이 펼쳐지고, 다시 누르면 접히는지 확인합니다.
- 폴더 수가 많을 때 페이지 전체가 아닌 폴더 목록 영역 내부에서만 스크롤되는지 확인합니다.
- 폴더 선택 후 목록이 접히고 선택된 폴더명이 요약 버튼에 반영되는지 확인합니다.

## 19. Home 축제 및 행사 카드 날짜 기준 동적 정렬

Home 화면의 `TRENDING.EVENTS` 영역이 일부 고정적인 축제 정보처럼 보이는 문제를 확인했습니다. 사용자가 메인에서 축제 및 행사 정보를 볼 때 현재 날짜를 기준으로 가까운 축제를 먼저 확인할 수 있도록 정렬 기준과 배지 표시를 보정했습니다.

### 19.1 원인

- Home 화면은 한국관광공사 `searchFestival2` 기반 데이터를 가져오지만, 화면에서는 조회 결과의 앞 3개만 표시했습니다.
- 연중 진행 행사처럼 시작일이 과거인 항목이 앞쪽에 남으면, 메인 화면이 동적인 행사 추천이 아니라 고정 카드처럼 보일 수 있었습니다.
- 날짜 배지도 `MM.DD` 중심으로 표시되어 오늘 기준으로 얼마나 가까운 행사인지 직관적으로 드러나지 않았습니다.

### 19.2 수정 방향

- Home 트렌딩 축제 조회 시 날짜 오름차순 기준을 명시했습니다.
- 조회 결과를 Home에서 한 번 더 재정렬해 오늘 이후 시작일이 가까운 축제를 우선 표시하도록 했습니다.
- 오늘 이후 시작 예정 축제가 부족할 경우, 진행 중인 축제는 종료일이 가까운 순서로 보완 표시하도록 했습니다.
- 축제 배지는 `TODAY`, `D-n`, `오늘 종료`, `진행중` 형태로 표시해 현재 날짜 기준의 의미를 더 명확히 했습니다.
- API 호출량은 기존 Home 트렌딩 미리보기 범위인 `poolMaxRows: 100`을 유지해 과도하게 늘리지 않았습니다.

### 19.3 검증 계획

- Home 화면 `축제 및 행사` 카드가 오늘 날짜 기준 가까운 예정 축제를 우선 표시하는지 확인합니다.
- 카드 배지가 `TODAY`, `D-n`, `진행중` 등 현재 날짜 기준 문구로 표시되는지 확인합니다.
- `/festivals` 전체 목록 이동 링크가 기존처럼 정상 동작하는지 확인합니다.

## 20. 모바일 위시리스트·내 활동 목록 접힘 처리

모바일 웹 환경에서 마이페이지의 전체 위시리스트와 내 활동의 최근 본 여행지 목록이 모두 펼쳐져 페이지 스크롤이 과도하게 길어지는 문제를 확인했습니다. 특히 iPhone 12 Pro 기준 화면에서는 상단 목록이 긴 경우 핵심 활동 탭과 본문으로 이동하기 전까지 불필요한 스크롤이 발생했습니다.

### 20.1 수정 방향

- `MyPage.jsx`의 위시리스트 카드 목록은 모바일에서 기본 3개만 먼저 표시하고, 저장 항목이 4개 이상이면 `전체 보기` / `접기` 버튼으로 목록을 확장하도록 변경했습니다.
- 데스크톱에서는 기존처럼 전체 위시리스트 카드가 표시되도록 유지했습니다.
- 폴더를 변경하거나 정렬 기준을 바꾸면 모바일 위시리스트 확장 상태를 초기화해 새 조건의 첫 3개 항목부터 확인할 수 있도록 했습니다.
- 위시리스트 카드 하단의 폴더명은 긴 이름으로 인해 버튼 영역이 밀리지 않도록 `truncate` 처리를 보강했습니다.
- `MyActivity.jsx`의 최근 본 여행지 목록은 모바일에서 3열 요약 카드로 표시하고, 4개 이상이면 `전체 보기` / `접기` 버튼을 제공하도록 변경했습니다.
- 내 활동 탭 영역은 모바일에서 가로 스크롤 가능한 탭 바 구조로 보정해 `BOARD COMMENTS`, `TRAVEL COMMENTS` 같은 긴 라벨이 화면 밖으로 잘려나가지 않도록 했습니다.

### 20.2 검증 계획

- 모바일 크기에서 마이페이지 진입 시 위시리스트 카드가 기본 3개만 표시되는지 확인합니다.
- `전체 보기`를 누르면 나머지 위시리스트 카드가 표시되고, `접기`를 누르면 다시 3개만 표시되는지 확인합니다.
- 폴더 선택 또는 정렬 변경 후 위시리스트 목록이 다시 요약 상태로 돌아오는지 확인합니다.
- 내 활동 페이지에서 최근 본 여행지가 기본 3개만 표시되고, 탭 버튼이 잘리지 않고 가로 스크롤되는지 확인합니다.

## 21. Home 추천 카드 데스크톱 3열 배치 복구

모바일 Home 카드 가독성을 보정한 뒤 데스크톱 화면에서 `지역 기반 추천`, `관심 지역 기반 즉흥 추천`, `축제 및 행사` 카드가 한 줄 3열이 아니라 왼쪽 2칸과 오른쪽 1칸의 비대칭 구조로 배치되는 문제를 확인했습니다. 이로 인해 기존 데스크톱 화면에서 한눈에 보이던 3개 추천 카드 중 두 번째 추천 카드가 아래로 밀려 Home 화면의 정보 밀도가 낮아졌습니다.

### 21.1 원인

- Home 추천 카드 영역이 `lg:grid-cols-3`와 내부 `lg:col-span-2` 래퍼를 함께 사용하고 있었습니다.
- 내부 래퍼가 첫 번째와 두 번째 카드를 별도 중첩 그리드로 묶으면서, 데스크톱 폭에서도 두 카드가 왼쪽 영역 안에서만 배치되었습니다.
- 모바일 대응 과정에서 카드의 세로 흐름은 안정화됐지만, 데스크톱 기준의 3개 카드 동일 행 배치가 깨졌습니다.

### 21.2 수정 방향

- 추천 카드 섹션의 최상위 그리드는 모바일 1열, 중간 화면 2열, 데스크톱 3열로 동작하도록 정리했습니다.
- 첫 번째와 두 번째 카드를 감싸는 래퍼는 데스크톱 `xl` 이상에서 `contents`로 풀어 부모 그리드의 직접 카드처럼 배치되도록 했습니다.
- 모바일과 태블릿에서는 기존처럼 카드가 자연스럽게 1열 또는 2열로 흐르도록 유지했습니다.
- `축제 및 행사` 카드는 별도 `lg:col-span-1` 지정 없이 부모 그리드 안에서 동일한 카드 단위로 배치되도록 조정했습니다.

### 21.3 검증 계획

- 데스크톱 폭에서 Home 추천 카드 3개가 한 줄에 `지역 기반 추천`, `관심 지역 기반 즉흥 추천`, `축제 및 행사` 순서로 표시되는지 확인합니다.
- 태블릿 폭에서 추천 카드 2개와 축제 카드가 자연스럽게 다음 줄로 흐르는지 확인합니다.
- 모바일 폭에서 카드가 1열로 쌓이고 이전에 보정한 문구 줄바꿈과 푸터·하단 메뉴 UI가 유지되는지 확인합니다.

## 22. README 최신 프로젝트 상태 반영

프로젝트 README가 2026-08-16 기준으로 머물러 있어, 이후 진행한 Gemini Functions 프록시 안정화, TourAPI 신규 여행지 알림, Firebase Storage 이미지 업로드, 모바일 UI 안정화, Home 축제 동적 정렬 등의 최신 상태가 충분히 드러나지 않는 문제를 확인했습니다. 또한 문서 톤이 다소 딱딱해 보여 공모전·포트폴리오 문서로 읽을 때 핵심 가치가 빠르게 전달되지 않았습니다.

### 22.1 수정 방향

- README 제목과 주요 섹션에 이모지를 추가해 문서 탐색성을 높였습니다.
- 배포 정보, 기술 스택, 시스템 구조에 Firebase Storage와 `syncTourApiUpdates` Scheduled Function을 반영했습니다.
- 주요 기능에 비회원 위시리스트 안내, 로그인 후 탐색 복귀, 여행 날짜 기반 AI 일정 생성, Firebase Storage 이미지 업로드, TourAPI 신규 여행지 알림을 추가했습니다.
- Firebase 데이터 구조에 `tourApiUpdates` 경로를 추가했습니다.
- 이미지 저장 정책 섹션을 신설해 프로필 이미지는 사용자별 고정 경로 덮어쓰기 방식으로 저장하고, 게시글 이미지는 게시글별 경로에 저장한다는 기준을 명시했습니다.
- 환경 변수와 Secret 섹션에 `TOUR_API_SERVICE_KEY`를 추가했습니다.
- 배포 섹션에 `syncTourApiUpdates` Functions 배포와 Storage Rules 배포 명령을 추가했습니다.
- 공모전 제출 준비 상태에 최근 안정화 작업과 남은 검증 항목을 최신화했습니다.
- README 하단의 최종 수정일을 `2026-08-25`로 갱신했습니다.

### 22.2 검증 계획

- README 링크가 실제 문서 경로와 일치하는지 확인합니다.
- README에 민감정보 원문이 포함되지 않았는지 확인합니다.
- 최신 구현 상태와 맞지 않는 과거 문구가 남아 있지 않은지 확인합니다.

## 23. 모바일 탐색·축제·AI 플래너 UI 안정화

모바일 웹 환경에서 여행지 탐색, 여행지 상세, 축제 정보, AI 플래너 화면을 확인한 결과 일부 레이아웃이 작은 화면 폭에 맞게 수축되지 않아 텍스트가 튀어나오거나 카드가 과도하게 길게 표시되는 문제가 확인되었습니다. 특히 여행지 탐색 상세 화면은 긴 여행지명이 히어로 영역을 밀어내며 오른쪽으로 잘리는 현상이 있었고, 탐색 목록과 축제 목록은 모바일에서 한 페이지에 표시되는 카드 수와 카드 크기가 커서 스크롤 부담이 컸습니다.

### 23.1 수정 방향

- 여행지 탐색 목록은 모바일에서 2열 카드와 6개 단위 페이지네이션을 사용하도록 조정했습니다.
- 데스크톱 탐색 목록은 기존 10개 단위와 2열 카드 구조를 유지해 화면 정보량을 보존했습니다.
- 여행지 탐색 필터 카드(`FILTERS.CONFIG`)는 모바일에서 기본 접힘 상태로 제공하고, `열기` / `접기` 버튼으로 지역·테마 필터를 확인하도록 변경했습니다.
- 축제 정보 목록은 모바일에서 2열 카드와 6개 단위 페이지네이션을 사용하도록 변경했습니다.
- 축제 카드의 모바일 제목, 날짜 배지, 주소, 타입 라벨 표시 밀도를 줄여 카드가 과도하게 길어지지 않도록 보정했습니다.
- 여행지 상세 화면의 히어로 제목은 모바일에서 줄바꿈과 최대 폭을 제한해 긴 여행지명이 화면 오른쪽으로 밀려나지 않도록 했습니다.
- 상세 본문 컨테이너의 모바일 좌우 여백을 줄여 작은 화면에서 내용 영역이 잘리지 않도록 했습니다.
- 모바일 하단 내비게이션은 `flex justify-around` 방식에서 7칸 고정 그리드로 변경해 HOME과 MY PAGE 양끝 여백이 더 균등하게 보이도록 했습니다.
- AI Planner 화면에서 사용자에게 보이는 주요 명칭은 `AI 여행 코스` 대신 `AI 여행 플래너`로 통일했습니다.
- AI 플래너의 Plan Mode 버튼은 모바일에서 1열로 쌓이도록 변경해 긴 버튼 문구가 카드 밖으로 튀어나오지 않도록 했습니다.
- 기존 저장 데이터 식별에 쓰이는 `[AI 여행 코스]` 레거시 prefix는 기존 문서 호환성을 위해 변경하지 않았습니다.

### 23.2 검증 결과

- `npm run build`를 실행해 프로덕션 빌드가 성공하는 것을 확인했습니다.
- 빌드 결과 기존 500kB 초과 번들 경고는 유지되었으며, 이번 UI 수정으로 인한 신규 빌드 오류는 발생하지 않았습니다.

### 23.3 추가 확인 필요

- iPhone 12 Pro 기준에서 여행지 탐색 목록이 2열·6개 단위로 표시되는지 확인합니다.
- 모바일에서 `FILTERS.CONFIG`가 기본 접힘 상태로 보이고, 열기 후 지역·테마·필터 실행 버튼이 정상 동작하는지 확인합니다.
- 모바일 축제 목록이 2열·6개 단위로 표시되고, 페이지네이션이 정상 동작하는지 확인합니다.
- 긴 여행지명 상세 화면에서 제목과 하트 버튼이 화면 밖으로 밀리지 않는지 확인합니다.
- AI 플래너 화면에서 `AI 여행 플래너` 명칭과 Plan Mode 버튼 줄바꿈이 자연스럽게 보이는지 확인합니다.

## 24. AI 플래너 모바일 시간 입력·미리보기 카드 보정

모바일 웹 환경에서 AI 플래너 조건 입력 폼을 확인한 결과 `START`, `END` 시간 입력 필드의 표시 폭이 좁아 `오전 10`, `오후 0`처럼 시간이 잘려 보이는 문제가 확인되었습니다. 또한 코스 생성 전 빈 미리보기 카드가 데스크톱 기준 높이를 그대로 유지해 모바일 화면에서 과도하게 큰 빈 영역을 차지했습니다.

### 24.1 원인

- `PACE`, `START`, `END` 입력 영역이 모든 화면에서 3열 그리드로 고정되어 있었습니다.
- 모바일 폭에서는 시간 입력 필드 내부의 AM/PM 표시와 시간 텍스트를 담기에 칸 너비가 부족했습니다.
- 생성 결과 미리보기 영역의 최소 높이가 데스크톱 기준 `620px`로 고정되어 있어 모바일에서 불필요한 스크롤을 만들었습니다.

### 24.2 수정 방향

- 모바일에서는 `PACE` 입력을 한 줄 전체에 배치하고, `START`와 `END` 시간 입력만 2열로 배치하도록 조정했습니다.
- 데스크톱에서는 기존처럼 `PACE`, `START`, `END`가 3열로 표시되도록 유지했습니다.
- 시간 입력 필드에 `min-w-0`을 적용해 작은 화면에서도 그리드 칸 안에서 자연스럽게 수축되도록 했습니다.
- 코스 생성 전 빈 미리보기 카드의 최소 높이는 모바일에서 `360px`, 데스크톱에서 `620px`로 분리했습니다.

### 24.3 검증 계획

- iPhone 12 Pro 기준에서 `START`, `END` 시간 값이 잘리지 않고 표시되는지 확인합니다.
- 모바일에서 코스 생성 전 미리보기 카드가 화면 대부분을 불필요하게 차지하지 않는지 확인합니다.
- 데스크톱에서 기존 AI 플래너 2단 구성과 미리보기 영역 높이가 유지되는지 확인합니다.

### 24.4 검증 결과

- `npm run build`를 실행해 프로덕션 빌드가 성공하는 것을 확인했습니다.
- 빌드 결과 기존 500kB 초과 번들 경고는 유지되었으며, 이번 UI 수정으로 인한 신규 빌드 오류는 발생하지 않았습니다.

## 25. 사용자 정보 수정 화면 모바일 레이아웃 보정

모바일 웹 환경에서 사용자 정보 수정 페이지를 확인한 결과 비밀번호 변경 카드의 제목과 보조 라벨(`// security_credentials`)이 한 줄에 무리하게 배치되면서 `비밀번호 변경` 문구가 어색하게 줄바꿈되는 문제가 확인되었습니다. 또한 카드 내부 여백이 데스크톱 기준으로 유지되어 작은 화면에서 입력 폼이 오른쪽으로 몰려 보였습니다.

### 25.1 수정 방향

- 설정 페이지의 최상위 여백을 모바일 기준 `px-4 py-6`으로 줄이고, 데스크톱에서는 기존 넓은 여백을 유지하도록 반응형으로 조정했습니다.
- 프로필 수정, 관심지역 설정, 비밀번호 변경 카드의 헤더를 모바일에서는 세로 배치로 전환했습니다.
- 헤더의 보조 라벨은 `break-all`을 적용해 작은 화면에서도 카드 밖으로 밀리지 않도록 했습니다.
- 카드 본문 여백을 모바일에서는 `p-5`, 큰 화면에서는 `p-8`로 분리했습니다.
- 저장 버튼 영역은 모바일에서 세로 배치와 전체 폭 버튼을 사용하고, 데스크톱에서는 기존처럼 메시지와 버튼을 좌우 배치하도록 유지했습니다.

### 25.2 검증 계획

- 모바일 폭에서 `비밀번호 변경` 제목이 글자 단위로 찢어지지 않고 자연스럽게 표시되는지 확인합니다.
- `// security_credentials` 라벨이 카드 오른쪽으로 밀려나지 않는지 확인합니다.
- 프로필 저장, 관심지역 저장, 비밀번호 변경 버튼이 모바일에서 전체 폭으로 표시되는지 확인합니다.
- 데스크톱 설정 화면의 카드 구조와 버튼 배치가 기존 사용성을 유지하는지 확인합니다.

### 25.3 검증 결과

- `npm run build`를 실행해 프로덕션 빌드가 성공하는 것을 확인했습니다.
- 빌드 결과 기존 500kB 초과 번들 경고는 유지되었으며, 이번 설정 화면 UI 수정으로 인한 신규 빌드 오류는 발생하지 않았습니다.

## 26. 모바일 페이지 상단 헤더 레이아웃 통일

모바일 웹 환경에서 게시판, AI 플래너, 여행지 탐색, 마이페이지의 상단 제목 영역을 비교한 결과 페이지마다 좌우 여백과 상단 간격, 제목 크기와 줄간격이 조금씩 달라 전체 UI가 일관되지 않아 보이는 문제가 확인되었습니다.

### 26.1 원인

- 공통 `PageHeader` 컴포넌트는 사용하고 있었지만, 각 페이지의 외부 컨테이너가 서로 다른 `padding`과 `margin` 기준을 사용하고 있었습니다.
- 일부 페이지는 모바일에서도 데스크톱 기준 `p-8`, `p-10` 여백을 사용해 제목 영역이 다른 페이지보다 오른쪽으로 밀려 보였습니다.
- `PageHeader`의 제목과 라벨에 모바일 전용 줄간격·줄바꿈 규칙이 부족해 긴 라벨이나 제목이 페이지별로 다르게 보였습니다.

### 26.2 수정 방향

- `PageHeader`의 모바일 제목 크기, 줄간격, 라벨 줄바꿈 기준을 공통화했습니다.
- 게시판, 여행지 탐색, 축제 정보, 마이페이지의 모바일 외부 여백을 `px-4 py-8` 계열로 맞췄습니다.
- 태블릿·데스크톱에서는 기존보다 넓은 여백을 유지하도록 `sm`, `lg` 반응형 클래스를 분리했습니다.
- 마이페이지는 모바일에서 좌측 위시리스트 헤더가 다른 페이지와 비슷한 위치에서 시작하도록 `main` 컨테이너 여백을 조정했습니다.

### 26.3 검증 계획

- 모바일 폭에서 게시판, 여행지 탐색, AI 플래너, 마이페이지의 제목 시작 위치와 상단 간격이 유사하게 보이는지 확인합니다.
- `// board.log`, `// ai_trip.planner`, `// travel_explore.exe`, `// wishlist.workspace` 라벨이 작은 화면에서 카드 밖으로 밀리지 않는지 확인합니다.
- 데스크톱 화면에서 기존 페이지 폭과 주요 레이아웃이 과도하게 좁아지지 않는지 확인합니다.

### 26.4 검증 결과

- `npm run build`를 실행해 프로덕션 빌드가 성공하는 것을 확인했습니다.
- 빌드 결과 기존 500kB 초과 번들 경고는 유지되었으며, 이번 모바일 헤더 레이아웃 수정으로 인한 신규 빌드 오류는 발생하지 않았습니다.
